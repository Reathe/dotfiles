{{ if eq .chezmoi.os "windows" -}}
setx XDG_CONFIG_HOME %userprofile%/.config 

@echo off
:: Enable sudo
sudo echo Sudo is enabled, you can just press enter.
echo You can also run the program as Administrator
pause

echo install winget packages

@echo on
{{ range .packages.windows.winget -}}
winget install -s winget --accept-package-agreements {{ . }}
{{ end }}
@echo off

echo install choco packages
@echo on
sudo "C:\ProgramData\chocolatey\choco.exe" install -y {{ range .packages.windows.choco -}}{{ . }} {{ end }}
@echo off

echo %errorlevel%
{{ end }}
